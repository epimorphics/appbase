# Set up default prefixes
prefixes = com.epimorphics.appbase.core.PrefixService
prefixes.prefixFile = src/test/textSearch/prefixes.ttl

# The source being queried
ssource              = com.epimorphics.appbase.data.impl.FileSparqlSource
ssource.files        = src/test/textSearch/data
ssource.textIndex    = rdfs:label,http://www.epimorphics.com/test/search/label

